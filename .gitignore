# ── Generated / build artifacts (not source) ──
__pycache__/
*.pyc
build/
dist/*
# 放行打包好的可执行文件，方便别人直接下载使用
!dist/小狗桌宠.exe

# ── Local work / session memory (explicitly excluded) ──
.workbuddy/

# ── Dev junk ──
err_exe.txt
err_py.txt
linedog_wip.txt
